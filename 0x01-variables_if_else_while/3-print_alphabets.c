#include <stdio>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;

	char d;

	c = 'a';
	d = 'A';
	while
		(c <= 'z') {
			putcher(c);
			c++;
		}
	while
		(d <= 'z') {
			putcher(d);
			d++;
		}
	putcher('\n);
	return (0);
}
