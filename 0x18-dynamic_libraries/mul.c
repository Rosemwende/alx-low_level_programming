#include <stdlib.h>
#include <stdio.h>

/**
 * mul - mul of two integers
 * @x: first int
 * @y: second int
 * Return: multiplication of two integers
 */
int mul(int x, int y)
{       
return (x * y);
}
