#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strcmp - This compares two strings.
 * @s1: The first string to be compared.
 * @s2: The second string to be compared.
 *
 * Return: An integer less than,
 * equal to, or greater than zero if
 * 's1' is found, respectively, to be
 * less than, to match, or be greater
 * than 's2'.
 */
int _strcmp(char *s1, char *s2)
{
return (strcmp(s1, s2));
}
