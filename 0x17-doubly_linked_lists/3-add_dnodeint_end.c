#include "lists.h"
/**
 * add_dnodeint_end - it adds a new node at
 * the end of a dlistint_t list
 * @head: head of the list
 * @n: value of the element
 * Return: the address of the new element, or NULL if it failed
